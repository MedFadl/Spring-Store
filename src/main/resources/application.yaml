spring:
  application:
    name: store

  datasource:
    url: jdbc:mariadb://localhost:3306/Store?createDatabaseIfNotExist=true
    username: root
    password: admin

  jpa:
    #hibernate:
    # ddl-auto: update
    show-sql: true

server:
  port: 8080

app:
  page:
    size: 10